# README 대표 이미지 디자인 메모

## 배경

현재 README는 텍스트만 있는 포트폴리오형 구조다. 사용자가 `/Users/sungjh/castle/image/Screenshot 2026-03-08 at 2.23.51 AM.png` 이미지를 README 대표 이미지처럼 상단에 배치해 달라고 요청했다.

## 목표

- 저장소 첫 화면에서 프로젝트 결과물이 바로 보이게 한다.
- README의 간결한 구조는 유지한다.
- 이미지 경로는 GitHub에서 안정적으로 보이도록 단순한 ASCII 파일명으로 정리한다.

## 접근안

### 1. 소개 아래 미리보기 섹션 추가

- 장점: 문서 흐름은 자연스럽다.
- 단점: 대표 이미지라는 인상이 약하다.

### 2. 제목 바로 아래 대표 이미지 배치

- 장점: 저장소 첫 화면에서 작업물이 즉시 보인다.
- 단점: 문서 상단 높이가 조금 늘어난다.

### 3. 하단 링크 근처에 이미지 배치

- 장점: 본문 길이는 유지된다.
- 단점: 포트폴리오 목적과 맞지 않는다.

## 선택

2번을 선택한다. 사용자는 "대표 이미지 처럼 해줘"라고 승인했다.

## 세부 설계

- 원본 스크린샷은 로컬에 유지한다.
- 추적용 파일은 `/Users/sungjh/castle/image/castle-overview.png`로 복사한다.
- 원본 스크린샷 경로는 `.gitignore`에 추가해 저장소 상태를 깔끔하게 유지한다.
- README 제목 아래에 Markdown 이미지 문법으로 대표 이미지를 넣는다.
- 본문 텍스트는 큰 구조를 유지하고, 필요하면 한두 문장만 다듬는다.
